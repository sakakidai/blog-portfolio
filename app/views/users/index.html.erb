<%= render "shared/default_top_image" %>

<div class='container'>
  <div class='row'>
    <% @users.each do |user| %>
      <div class='col-lg-3 col-6'>
        <%= render 'user', user: user %>
      </div>
    <% end %>
  </div>
  <%= paginate @users %>
</div>